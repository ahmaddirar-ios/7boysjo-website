# Models (Phase 2 scaffold)
DB models wrap PDO + JSON fallback.
- Product.php -> load_products() / save_products()
- Visitor.php -> load_visitors()
- Employee.php
- Quote.php
Currently implemented inline in config.php for zero-downtime. Will be extracted here.
